import {types} from '../types';

const initialState = {
    tablesSelect: []
}
export function tablesR(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}